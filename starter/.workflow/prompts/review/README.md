# Review Prompts

Store one immutable complete prompt per approved task:

```text
PROMPT_<step-id>_<timestamp>.md
```

Do not create a separate short-prompt file. Direct messages and manual copy prompts should point to the complete prompt or carry its exact content.
