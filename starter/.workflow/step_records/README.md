# Step Records

Store one result record per prompt:

```text
<step-id>_<timestamp>/STEP_RESULT.md
```

Place actual task outputs in the project structure appropriate to the task. The result record points to them; it does not duplicate large artifacts.
